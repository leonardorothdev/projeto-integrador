export const createStudentsTableQuery = `
    CREATE TABLE IF NOT EXISTS students (
        id SERIAL PRIMARY KEY,
        name VARCHAR(100) NOT NULL,
        birth_date DATE NOT NULL,
        age INTEGER NOT NULL,
        institution VARCHAR(100) NOT NULL,
        grade VARCHAR(50) NOT NULL,
        nationality VARCHAR(50) NOT NULL,
        hometown VARCHAR(100) NOT NULL,
        state VARCHAR(50) NOT NULL,
        marital_status VARCHAR(50) NOT NULL,
        profession VARCHAR(100) NOT NULL,
        sex VARCHAR(10) NOT NULL CHECK (sex IN ('feminino', 'masculino')),
        responsible_name VARCHAR(100) NOT NULL,
        responsible_contact VARCHAR(50) NOT NULL,
        additional_responsible_name VARCHAR(100),
        additional_responsible_contact VARCHAR(50),
        cpf VARCHAR(14) NOT NULL UNIQUE,
        rg VARCHAR(20) NOT NULL UNIQUE,
        uf VARCHAR(2) NOT NULL,
        address TEXT NOT NULL,
        has_health_plan BOOLEAN NOT NULL DEFAULT FALSE,
        health_plan_name VARCHAR(100),
        uses_medication BOOLEAN NOT NULL DEFAULT FALSE,
        medication_name TEXT,
        has_allergy BOOLEAN NOT NULL DEFAULT FALSE,
        allergy_type TEXT,
        has_special_needs BOOLEAN NOT NULL DEFAULT FALSE,
        special_needs_type TEXT,
        blood_type VARCHAR(10),
        image_authorization BOOLEAN NOT NULL DEFAULT FALSE,
        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
    );
`;
